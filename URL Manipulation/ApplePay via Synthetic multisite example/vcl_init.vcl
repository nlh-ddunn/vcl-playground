# add domains and their verification strings here
# note: you could create this table in the admin UI to allow for easy updates
table apple_pay_domains {
  "example.com": "you can add apple pay verification directly to Fastly without uploading files",
  "example.net": "even on multiple domains",
}
